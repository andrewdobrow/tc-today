# Breaking Article Image Override — August 6, 2026

Pins `2026-08-06-martin-county-sheriffs-office-seeks-public-help-finding-missing-14-year-old-auti` to `https://treasurecoast.today/images/ethan-boyd.png` across the visible article image, homepage/category placements, RSS, Open Graph, Twitter metadata, NewsArticle JSON-LD, `data.json`, and `archive.json`.

The durable override is stored in `data/article-image-overrides.json` and reapplied after archive recovery and permalink rebinding so fallback rotation cannot replace it on later workflow runs.

Important: the supplied repository did not contain `images/ethan-boyd.png`; the override assumes that asset is present at the supplied public URL.
