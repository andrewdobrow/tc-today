# Editorial image pool

Reusable fallback imagery for Treasure Coast Today lives in this directory.

The generator discovers supported image files automatically. It prefers WebP, then JPG/JPEG, then PNG when multiple formats share the same filename stem.

See `/EDITORIAL_IMAGE_POOL.md` for selection order, rotation behavior, folder mappings and reports.

Article-specific custom images do not belong in this pool.
