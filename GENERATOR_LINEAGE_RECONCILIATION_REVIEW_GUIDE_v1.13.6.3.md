# v1.13.6.3 generator lineage reconciliation review

This release is a reconciliation release, not another isolated final-gate patch.

## Before production

Run **Test Editorial Engine**. The exact workflow suite must be fully green, including:

- Martin cocaine duplicate regression;
- subscriber chrome launch candidate;
- generator active-release coherence;
- forward publication identity;
- registry pressure serialization;
- global/live category incident identity.

## Production checkpoints

On **Update Treasure Coast Today**, verify in order:

1. registry writes proceed without the 50 MiB failure;
2. category generation completes;
3. publication identity and story regression gates pass;
4. `Forward live identity contract PASSED`;
5. `Live category canonical contract PASSED`;
6. final image/county/nonstory/permalink/RSS/sitemap gates complete;
7. the workflow reaches deployment/commit rather than stopping in `scripts/generate.py`.

If any final contract fails, the live-category contract now emits exact category/placement/identity diagnostics rather than only an aggregate count.
