# Patch Notes — v1.12.1.1

## Newsletter inline form

- Adds the supplied Kit inline form UID `30e15672d3` beneath the active category hero on the homepage/category surface.
- Adds the same inline signup once after the article body on every generated article page.
- Keeps the existing sticky-bar form UID `4edef44197` separate; each Kit form initializes once per eligible page.
- Adds a restrained responsive wrapper so the Kit form follows TCT content widths, spacing and corner treatment without overriding Kit's editable internal design.
- Does not change editorial identity, ranking, archive, custom-article or permalink behavior.
